# 🔀 CHE·NU™ CROSS-SPHERE IMPORT SYSTEM
## Navigation, Recherche & Importation Inter-Sphères

---

# 📐 CONCEPT FONDAMENTAL

```
╔═══════════════════════════════════════════════════════════════════════════════╗
║                    PRINCIPE: SÉPARATION AVEC PONTS                            ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║                                                                               ║
║   ❌ CE QU'ON NE FAIT PAS:                                                   ║
║   • Mélanger les données entre sphères                                       ║
║   • Copier/dupliquer les données                                             ║
║   • Perdre la traçabilité de l'origine                                       ║
║                                                                               ║
║   ✅ CE QU'ON FAIT:                                                          ║
║   • Créer des RÉFÉRENCES (liens) vers d'autres sphères                       ║
║   • Garder l'original dans sa sphère source                                  ║
║   • Importer une VUE dans un emplacement défini                              ║
║   • Tracer l'origine et les permissions                                      ║
║                                                                               ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║                                                                               ║
║   MÉTAPHORE: BIBLIOTHÈQUE                                                    ║
║                                                                               ║
║   • Chaque sphère = Une bibliothèque séparée                                 ║
║   • Import = Créer une FICHE DE RÉFÉRENCE vers un livre d'une autre biblio  ║
║   • Le livre original reste dans sa bibliothèque                             ║
║   • La fiche indique où trouver le livre et comment y accéder               ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝
```

---

# 🎯 FLUX UTILISATEUR

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ÉTAPE 1: TRAVAILLER DANS UNE SPHÈRE                                      │
│   ════════════════════════════════════                                      │
│                                                                             │
│   L'utilisateur est dans: 💼 BUSINESS                                       │
│   Travaille sur: Projet Client XYZ                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ÉTAPE 2: OUVRIR LE PANNEAU D'IMPORT CROSS-SPHERE                         │
│   ════════════════════════════════════════════════                          │
│                                                                             │
│   [🔀 Importer depuis autre sphère]                                        │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                    IMPORT CROSS-SPHERE                              │  │
│   │                                                                     │  │
│   │   Sphère active: 💼 Business                                       │  │
│   │   Contexte: Projet Client XYZ                                      │  │
│   │                                                                     │  │
│   │   ─────────────────────────────────────────────────                │  │
│   │                                                                     │  │
│   │   Naviguer vers:                                                   │  │
│   │   ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │  │
│   │   │ 🏠  │ │ 🏛️  │ │ 🎨  │ │ 👥  │ │ 📱  │ │ 🎬  │ │ 🤝  │       │  │
│   │   │Perso│ │Gouv │ │Créa │ │Comm │ │Soci │ │Ent. │ │Team │       │  │
│   │   └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘       │  │
│   │                                                                     │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ÉTAPE 3: NAVIGUER & RECHERCHER DANS LA SPHÈRE SOURCE                     │
│   ══════════════════════════════════════════════════                        │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │  🎨 CREATIVE STUDIO                           [Mode Import Actif]  │  │
│   │                                                                     │  │
│   │  🔍 Rechercher: [logo client xyz____________] [🔎]                 │  │
│   │                                                                     │  │
│   │  Résultats:                                                        │  │
│   │  ┌─────────────────────────────────────────────────────────────┐  │  │
│   │  │ □ 📄 Thread: "Brainstorm Logo Client XYZ"                   │  │  │
│   │  │   Créé: 15 déc 2024 | 12 messages | 3 décisions             │  │  │
│   │  ├─────────────────────────────────────────────────────────────┤  │  │
│   │  │ □ 📁 Projet: "Identité Visuelle XYZ"                        │  │  │
│   │  │   5 fichiers | Dernière modif: 14 déc 2024                  │  │  │
│   │  ├─────────────────────────────────────────────────────────────┤  │  │
│   │  │ □ 📝 Note: "Feedback client sur couleurs"                   │  │  │
│   │  │   Créé: 10 déc 2024                                         │  │  │
│   │  └─────────────────────────────────────────────────────────────┘  │  │
│   │                                                                     │  │
│   │  [Sélectionner pour import]                                        │  │
│   │                                                                     │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ÉTAPE 4: DÉFINIR L'EMPLACEMENT D'IMPORTATION                             │
│   ════════════════════════════════════════════                              │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                 📍 EMPLACEMENT D'IMPORTATION                        │  │
│   │                                                                     │  │
│   │   Élément sélectionné:                                             │  │
│   │   📄 Thread: "Brainstorm Logo Client XYZ" (depuis 🎨 Creative)     │  │
│   │                                                                     │  │
│   │   ─────────────────────────────────────────────────                │  │
│   │                                                                     │  │
│   │   Importer dans 💼 Business:                                       │  │
│   │                                                                     │  │
│   │   📂 Projet Client XYZ                                             │  │
│   │   ├── 📁 Documents                                                 │  │
│   │   ├── 📁 Références ← [SÉLECTIONNÉ]                               │  │
│   │   │   └── 🔗 (nouvel import ici)                                  │  │
│   │   ├── 📁 Communications                                            │  │
│   │   └── 📁 Livrables                                                 │  │
│   │                                                                     │  │
│   │   Ou créer nouveau dossier: [____________________] [+ Créer]       │  │
│   │                                                                     │  │
│   │   ─────────────────────────────────────────────────                │  │
│   │                                                                     │  │
│   │   Options d'import:                                                │  │
│   │   ☑ Notifier quand l'original est modifié                         │  │
│   │   ☐ Copier les pièces jointes (pas seulement référencer)          │  │
│   │   ☑ Ajouter aux favoris                                           │  │
│   │                                                                     │  │
│   │               [ Annuler ]  [ ✓ Confirmer Import ]                  │  │
│   │                                                                     │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ÉTAPE 5: RÉSULTAT - RÉFÉRENCE CRÉÉE                                      │
│   ═══════════════════════════════════                                       │
│                                                                             │
│   💼 BUSINESS > Projet Client XYZ > Références                             │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │ 🔗 Brainstorm Logo Client XYZ                                      │  │
│   │                                                                     │  │
│   │ ┌─────────────────────────────────────────────────────────────┐   │  │
│   │ │ 📍 Source: 🎨 Creative Studio                               │   │  │
│   │ │ 📅 Importé: 18 déc 2024                                     │   │  │
│   │ │ 🔄 Dernière sync: Il y a 2 minutes                          │   │  │
│   │ │                                                              │   │  │
│   │ │ [Ouvrir l'original] [Rafraîchir] [Retirer référence]        │   │  │
│   │ └─────────────────────────────────────────────────────────────┘   │  │
│   │                                                                     │  │
│   │ Aperçu du contenu:                                                 │  │
│   │ ────────────────────                                               │  │
│   │ • 12 messages dans le thread                                       │  │
│   │ • 3 décisions prises                                               │  │
│   │ • 5 fichiers attachés                                              │  │
│   │                                                                     │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

# 🗃️ MODÈLE DE DONNÉES

## Schema SQL

```sql
-- ═══════════════════════════════════════════════════════════════════
-- CROSS-SPHERE IMPORT SYSTEM
-- Extension au schema CHE·NU existant
-- ═══════════════════════════════════════════════════════════════════

-- ═══════════════════════════════════════════════════════════════════
-- TABLE: RÉFÉRENCES CROSS-SPHERE
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE cross_sphere_references (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    
    -- Propriétaire de la référence
    owner_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    
    -- SOURCE (d'où vient l'élément)
    source_sphere_id VARCHAR(20) NOT NULL,
    source_item_type VARCHAR(50) NOT NULL,  -- 'thread', 'dataspace', 'note', 'task', 'document', 'project'
    source_item_id UUID NOT NULL,
    source_item_title VARCHAR(255),         -- Titre au moment de l'import (pour affichage même si déconnecté)
    
    -- DESTINATION (où est placée la référence)
    target_sphere_id VARCHAR(20) NOT NULL,
    target_location_type VARCHAR(50) NOT NULL,  -- 'dataspace', 'folder', 'project', 'root'
    target_location_id UUID,                     -- NULL si root de la sphère
    target_location_path TEXT,                   -- Chemin lisible pour UI
    
    -- MÉTADONNÉES
    reference_type VARCHAR(20) DEFAULT 'link',  -- 'link' (référence), 'mirror' (sync bidirectionnelle), 'snapshot' (copie figée)
    display_name VARCHAR(255),                  -- Nom personnalisé de la référence
    notes TEXT,                                 -- Notes de l'utilisateur sur pourquoi importé
    tags TEXT[],
    color VARCHAR(20),                          -- Couleur personnalisée pour identification
    
    -- SYNCHRONISATION
    sync_enabled BOOLEAN DEFAULT TRUE,
    last_sync_at TIMESTAMP WITH TIME ZONE,
    source_last_modified_at TIMESTAMP WITH TIME ZONE,
    sync_status VARCHAR(20) DEFAULT 'synced',   -- 'synced', 'pending', 'conflict', 'disconnected'
    
    -- NOTIFICATIONS
    notify_on_change BOOLEAN DEFAULT TRUE,
    notify_on_delete BOOLEAN DEFAULT TRUE,
    
    -- PERMISSIONS HÉRITÉES
    inherited_permissions JSONB DEFAULT '{}',   -- Permissions depuis la source
    
    -- AUDIT
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    created_by UUID REFERENCES users(id),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    
    -- CONTRAINTES
    CONSTRAINT different_spheres CHECK (source_sphere_id != target_sphere_id),
    CONSTRAINT valid_reference_type CHECK (reference_type IN ('link', 'mirror', 'snapshot'))
);

-- Index pour recherche rapide
CREATE INDEX idx_cross_ref_owner ON cross_sphere_references(owner_id);
CREATE INDEX idx_cross_ref_source ON cross_sphere_references(source_sphere_id, source_item_id);
CREATE INDEX idx_cross_ref_target ON cross_sphere_references(target_sphere_id, target_location_id);
CREATE INDEX idx_cross_ref_sync ON cross_sphere_references(sync_status) WHERE sync_enabled = TRUE;

-- ═══════════════════════════════════════════════════════════════════
-- TABLE: HISTORIQUE DES IMPORTS
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE cross_sphere_import_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    reference_id UUID REFERENCES cross_sphere_references(id) ON DELETE CASCADE,
    
    action VARCHAR(50) NOT NULL,  -- 'created', 'synced', 'updated', 'conflict_resolved', 'disconnected', 'deleted'
    action_by UUID REFERENCES users(id),
    action_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    
    -- Détails de l'action
    details JSONB DEFAULT '{}',
    
    -- Snapshot des données au moment de l'action (pour audit)
    source_snapshot JSONB,
    
    -- Pour résolution de conflits
    conflict_data JSONB,
    resolution_method VARCHAR(50)  -- 'keep_source', 'keep_target', 'manual', 'merge'
);

CREATE INDEX idx_import_history_ref ON cross_sphere_import_history(reference_id);
CREATE INDEX idx_import_history_action ON cross_sphere_import_history(action, action_at);

-- ═══════════════════════════════════════════════════════════════════
-- TABLE: EMPLACEMENTS FAVORIS D'IMPORT
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE import_location_favorites (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    
    sphere_id VARCHAR(20) NOT NULL,
    location_type VARCHAR(50) NOT NULL,
    location_id UUID,
    location_path TEXT,
    display_name VARCHAR(255),
    
    -- Statistiques d'utilisation
    use_count INTEGER DEFAULT 0,
    last_used_at TIMESTAMP WITH TIME ZONE,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    
    UNIQUE(user_id, sphere_id, location_id)
);

-- ═══════════════════════════════════════════════════════════════════
-- TABLE: RÈGLES D'IMPORT AUTOMATIQUE
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE auto_import_rules (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    
    name VARCHAR(255) NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    
    -- Conditions de déclenchement
    trigger_sphere_id VARCHAR(20),           -- Sphère source à surveiller
    trigger_item_type VARCHAR(50),           -- Type d'item à surveiller
    trigger_conditions JSONB DEFAULT '{}',   -- Conditions (tags, mots-clés, etc.)
    
    -- Action
    target_sphere_id VARCHAR(20) NOT NULL,
    target_location_id UUID,
    target_location_path TEXT,
    import_options JSONB DEFAULT '{}',
    
    -- Statistiques
    times_triggered INTEGER DEFAULT 0,
    last_triggered_at TIMESTAMP WITH TIME ZONE,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- ═══════════════════════════════════════════════════════════════════
-- VUES UTILES
-- ═══════════════════════════════════════════════════════════════════

-- Vue des références avec statut de sync
CREATE VIEW cross_sphere_references_status AS
SELECT 
    csr.*,
    CASE 
        WHEN csr.sync_enabled = FALSE THEN 'disabled'
        WHEN csr.source_last_modified_at > csr.last_sync_at THEN 'needs_sync'
        ELSE 'synced'
    END as computed_sync_status,
    EXTRACT(EPOCH FROM (NOW() - csr.last_sync_at)) as seconds_since_sync
FROM cross_sphere_references csr;

-- Vue des imports par sphère
CREATE VIEW sphere_import_summary AS
SELECT 
    target_sphere_id as sphere_id,
    COUNT(*) as total_imports,
    COUNT(*) FILTER (WHERE sync_status = 'synced') as synced_count,
    COUNT(*) FILTER (WHERE sync_status = 'pending') as pending_count,
    COUNT(*) FILTER (WHERE sync_status = 'conflict') as conflict_count,
    COUNT(DISTINCT source_sphere_id) as source_spheres_count
FROM cross_sphere_references
GROUP BY target_sphere_id;
```

---

# 🔌 API ENDPOINTS

```python
# api/routes/cross_sphere.py

from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.ext.asyncio import AsyncSession
from typing import List, Optional
from uuid import UUID
from pydantic import BaseModel
from datetime import datetime

router = APIRouter(prefix="/api/cross-sphere", tags=["Cross-Sphere Import"])

# ═══════════════════════════════════════════════════════════════════
# SCHEMAS
# ═══════════════════════════════════════════════════════════════════

class SearchResult(BaseModel):
    item_id: UUID
    item_type: str
    title: str
    description: Optional[str]
    sphere_id: str
    created_at: datetime
    updated_at: datetime
    preview: Optional[dict]  # Aperçu du contenu
    can_import: bool
    already_imported: bool

class ImportLocation(BaseModel):
    location_type: str  # 'dataspace', 'folder', 'project', 'root'
    location_id: Optional[UUID]
    location_path: str
    display_name: str

class CreateImportRequest(BaseModel):
    source_sphere_id: str
    source_item_type: str
    source_item_id: UUID
    target_location: ImportLocation
    display_name: Optional[str]
    notes: Optional[str]
    notify_on_change: bool = True
    reference_type: str = "link"  # 'link', 'mirror', 'snapshot'

class CrossSphereReference(BaseModel):
    id: UUID
    source_sphere_id: str
    source_item_type: str
    source_item_id: UUID
    source_item_title: str
    target_sphere_id: str
    target_location_path: str
    display_name: str
    sync_status: str
    last_sync_at: Optional[datetime]
    created_at: datetime

# ═══════════════════════════════════════════════════════════════════
# NAVIGATION & RECHERCHE
# ═══════════════════════════════════════════════════════════════════

@router.get("/browse/{sphere_id}")
async def browse_sphere_for_import(
    sphere_id: str,
    user_id: UUID,
    location_id: Optional[UUID] = None,
    item_types: List[str] = Query(default=["thread", "dataspace", "note", "task", "document"]),
    db: AsyncSession = Depends(get_db)
):
    """
    Parcourir une sphère pour trouver des éléments à importer.
    Retourne la structure hiérarchique de la sphère.
    """
    # Vérifier les permissions d'accès à la sphère
    has_access = await check_sphere_access(db, user_id, sphere_id)
    if not has_access:
        raise HTTPException(status_code=403, detail="No access to this sphere")
    
    # Récupérer la structure
    items = await get_sphere_items(
        db, 
        user_id=user_id,
        sphere_id=sphere_id, 
        parent_id=location_id,
        item_types=item_types
    )
    
    # Marquer les éléments déjà importés
    current_sphere = await get_current_sphere(db, user_id)
    for item in items:
        item.already_imported = await is_already_imported(
            db, user_id, item.item_id, current_sphere
        )
    
    return {
        "sphere_id": sphere_id,
        "location_id": location_id,
        "items": items,
        "breadcrumb": await get_breadcrumb(db, sphere_id, location_id)
    }

@router.get("/search")
async def search_across_spheres(
    user_id: UUID,
    query: str,
    source_spheres: List[str] = Query(default=None),  # None = toutes les sphères accessibles
    item_types: List[str] = Query(default=["thread", "dataspace", "note", "document"]),
    limit: int = 20,
    offset: int = 0,
    db: AsyncSession = Depends(get_db)
):
    """
    Rechercher des éléments dans une ou plusieurs sphères.
    Exclut la sphère active (on n'importe pas depuis soi-même).
    """
    # Déterminer les sphères à rechercher
    current_sphere = await get_current_sphere(db, user_id)
    accessible_spheres = await get_accessible_spheres(db, user_id)
    
    if source_spheres:
        search_spheres = [s for s in source_spheres if s in accessible_spheres and s != current_sphere]
    else:
        search_spheres = [s for s in accessible_spheres if s != current_sphere]
    
    # Recherche
    results = await search_items(
        db,
        user_id=user_id,
        query=query,
        spheres=search_spheres,
        item_types=item_types,
        limit=limit,
        offset=offset
    )
    
    # Enrichir avec statut d'import
    for result in results:
        result.already_imported = await is_already_imported(
            db, user_id, result.item_id, current_sphere
        )
    
    return {
        "query": query,
        "searched_spheres": search_spheres,
        "results": results,
        "total": len(results),
        "offset": offset
    }

# ═══════════════════════════════════════════════════════════════════
# GESTION DES EMPLACEMENTS
# ═══════════════════════════════════════════════════════════════════

@router.get("/locations/{sphere_id}")
async def get_import_locations(
    sphere_id: str,
    user_id: UUID,
    parent_id: Optional[UUID] = None,
    db: AsyncSession = Depends(get_db)
):
    """
    Récupère les emplacements possibles pour un import dans une sphère.
    Retourne la structure de dossiers/projets/dataspaces.
    """
    locations = await get_available_locations(
        db, user_id, sphere_id, parent_id
    )
    
    favorites = await get_favorite_locations(db, user_id, sphere_id)
    recent = await get_recent_import_locations(db, user_id, sphere_id, limit=5)
    
    return {
        "sphere_id": sphere_id,
        "parent_id": parent_id,
        "locations": locations,
        "favorites": favorites,
        "recent": recent,
        "can_create_new": True
    }

@router.post("/locations/{sphere_id}/create")
async def create_import_location(
    sphere_id: str,
    user_id: UUID,
    parent_id: Optional[UUID],
    name: str,
    location_type: str = "folder",
    db: AsyncSession = Depends(get_db)
):
    """
    Crée un nouvel emplacement (dossier) pour les imports.
    """
    location = await create_location(
        db,
        user_id=user_id,
        sphere_id=sphere_id,
        parent_id=parent_id,
        name=name,
        location_type=location_type
    )
    
    return {
        "status": "created",
        "location": location
    }

@router.post("/locations/favorite")
async def add_favorite_location(
    user_id: UUID,
    sphere_id: str,
    location_id: UUID,
    display_name: Optional[str] = None,
    db: AsyncSession = Depends(get_db)
):
    """
    Ajoute un emplacement aux favoris pour imports rapides.
    """
    await add_to_favorites(db, user_id, sphere_id, location_id, display_name)
    return {"status": "added"}

# ═══════════════════════════════════════════════════════════════════
# IMPORT & RÉFÉRENCES
# ═══════════════════════════════════════════════════════════════════

@router.post("/import")
async def create_cross_sphere_import(
    user_id: UUID,
    request: CreateImportRequest,
    db: AsyncSession = Depends(get_db)
):
    """
    Crée une référence cross-sphere (import).
    L'élément original reste dans sa sphère source.
    Une référence est créée dans l'emplacement cible.
    """
    # Vérifications
    current_sphere = await get_current_sphere(db, user_id)
    
    # Vérifier qu'on n'importe pas depuis la même sphère
    if request.source_sphere_id == current_sphere:
        raise HTTPException(
            status_code=400, 
            detail="Cannot import from the same sphere"
        )
    
    # Vérifier l'accès à la source
    has_source_access = await check_item_access(
        db, user_id, request.source_item_id, request.source_sphere_id
    )
    if not has_source_access:
        raise HTTPException(status_code=403, detail="No access to source item")
    
    # Vérifier que l'élément n'est pas déjà importé au même endroit
    existing = await check_existing_import(
        db, user_id, request.source_item_id, 
        request.target_location.location_id
    )
    if existing:
        raise HTTPException(
            status_code=409, 
            detail="Item already imported at this location"
        )
    
    # Récupérer les infos de l'élément source
    source_item = await get_item_details(
        db, request.source_item_id, request.source_sphere_id
    )
    
    # Créer la référence
    reference = await create_reference(
        db,
        owner_id=user_id,
        source_sphere_id=request.source_sphere_id,
        source_item_type=request.source_item_type,
        source_item_id=request.source_item_id,
        source_item_title=source_item.title,
        target_sphere_id=current_sphere,
        target_location_type=request.target_location.location_type,
        target_location_id=request.target_location.location_id,
        target_location_path=request.target_location.location_path,
        reference_type=request.reference_type,
        display_name=request.display_name or source_item.title,
        notes=request.notes,
        notify_on_change=request.notify_on_change
    )
    
    # Enregistrer dans l'historique
    await log_import_action(
        db, 
        reference_id=reference.id,
        action="created",
        user_id=user_id,
        details={"source_title": source_item.title}
    )
    
    # Mettre à jour les favoris/récents
    await update_location_usage(
        db, user_id, current_sphere, request.target_location.location_id
    )
    
    return {
        "status": "imported",
        "reference": reference
    }

@router.get("/references")
async def get_user_references(
    user_id: UUID,
    sphere_id: Optional[str] = None,
    sync_status: Optional[str] = None,
    db: AsyncSession = Depends(get_db)
):
    """
    Récupère toutes les références cross-sphere de l'utilisateur.
    Peut filtrer par sphère cible et statut de synchronisation.
    """
    references = await get_references(
        db,
        owner_id=user_id,
        target_sphere_id=sphere_id,
        sync_status=sync_status
    )
    
    return {
        "references": references,
        "total": len(references),
        "by_sphere": group_by_sphere(references),
        "sync_summary": {
            "synced": len([r for r in references if r.sync_status == "synced"]),
            "pending": len([r for r in references if r.sync_status == "pending"]),
            "conflict": len([r for r in references if r.sync_status == "conflict"])
        }
    }

@router.get("/references/{reference_id}")
async def get_reference_details(
    reference_id: UUID,
    user_id: UUID,
    db: AsyncSession = Depends(get_db)
):
    """
    Récupère les détails d'une référence avec aperçu du contenu source.
    """
    reference = await get_reference(db, reference_id, user_id)
    if not reference:
        raise HTTPException(status_code=404, detail="Reference not found")
    
    # Récupérer l'aperçu du contenu source
    source_preview = await get_source_preview(
        db, reference.source_item_id, reference.source_sphere_id
    )
    
    # Historique des synchronisations
    history = await get_reference_history(db, reference_id, limit=10)
    
    return {
        "reference": reference,
        "source_preview": source_preview,
        "history": history
    }

@router.put("/references/{reference_id}")
async def update_reference(
    reference_id: UUID,
    user_id: UUID,
    display_name: Optional[str] = None,
    notes: Optional[str] = None,
    notify_on_change: Optional[bool] = None,
    db: AsyncSession = Depends(get_db)
):
    """
    Met à jour les paramètres d'une référence.
    """
    reference = await get_reference(db, reference_id, user_id)
    if not reference:
        raise HTTPException(status_code=404, detail="Reference not found")
    
    updated = await update_reference_settings(
        db, reference_id,
        display_name=display_name,
        notes=notes,
        notify_on_change=notify_on_change
    )
    
    return {"status": "updated", "reference": updated}

@router.delete("/references/{reference_id}")
async def remove_reference(
    reference_id: UUID,
    user_id: UUID,
    db: AsyncSession = Depends(get_db)
):
    """
    Supprime une référence (l'original n'est PAS affecté).
    """
    reference = await get_reference(db, reference_id, user_id)
    if not reference:
        raise HTTPException(status_code=404, detail="Reference not found")
    
    await delete_reference(db, reference_id)
    
    await log_import_action(
        db, reference_id=reference_id,
        action="deleted",
        user_id=user_id
    )
    
    return {"status": "deleted", "message": "Reference removed. Original item unchanged."}

# ═══════════════════════════════════════════════════════════════════
# SYNCHRONISATION
# ═══════════════════════════════════════════════════════════════════

@router.post("/references/{reference_id}/sync")
async def sync_reference(
    reference_id: UUID,
    user_id: UUID,
    db: AsyncSession = Depends(get_db)
):
    """
    Force la synchronisation d'une référence avec sa source.
    """
    reference = await get_reference(db, reference_id, user_id)
    if not reference:
        raise HTTPException(status_code=404, detail="Reference not found")
    
    # Récupérer les données source actuelles
    source_data = await get_source_data(
        db, reference.source_item_id, reference.source_sphere_id
    )
    
    # Mettre à jour le statut de sync
    await update_sync_status(
        db, reference_id,
        last_sync_at=datetime.utcnow(),
        source_last_modified_at=source_data.updated_at,
        sync_status="synced"
    )
    
    await log_import_action(
        db, reference_id=reference_id,
        action="synced",
        user_id=user_id,
        source_snapshot=source_data.to_snapshot()
    )
    
    return {"status": "synced", "source_data": source_data}

@router.post("/sync-all")
async def sync_all_references(
    user_id: UUID,
    sphere_id: Optional[str] = None,
    db: AsyncSession = Depends(get_db)
):
    """
    Synchronise toutes les références de l'utilisateur (ou d'une sphère).
    """
    references = await get_references(
        db, owner_id=user_id, target_sphere_id=sphere_id,
        sync_enabled=True
    )
    
    results = []
    for ref in references:
        try:
            await sync_reference(ref.id, user_id, db)
            results.append({"id": ref.id, "status": "synced"})
        except Exception as e:
            results.append({"id": ref.id, "status": "error", "error": str(e)})
    
    return {
        "total": len(results),
        "synced": len([r for r in results if r["status"] == "synced"]),
        "errors": len([r for r in results if r["status"] == "error"]),
        "results": results
    }

# ═══════════════════════════════════════════════════════════════════
# NAVIGATION DIRECTE VERS SOURCE
# ═══════════════════════════════════════════════════════════════════

@router.get("/references/{reference_id}/open-source")
async def get_source_navigation(
    reference_id: UUID,
    user_id: UUID,
    db: AsyncSession = Depends(get_db)
):
    """
    Retourne les informations de navigation pour ouvrir l'élément source.
    Ne change pas de sphère active, ouvre dans une vue temporaire.
    """
    reference = await get_reference(db, reference_id, user_id)
    if not reference:
        raise HTTPException(status_code=404, detail="Reference not found")
    
    return {
        "source_sphere_id": reference.source_sphere_id,
        "source_item_type": reference.source_item_type,
        "source_item_id": reference.source_item_id,
        "navigation_url": f"/sphere/{reference.source_sphere_id}/{reference.source_item_type}/{reference.source_item_id}",
        "open_mode": "overlay"  # Ouvre en overlay, ne change pas la sphère active
    }
```

---

# 🧩 COMPOSANTS REACT

## Import Panel Component

```tsx
// components/cross-sphere/CrossSphereImportPanel.tsx

import React, { useState, useCallback, useEffect } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { useTheme } from '@/contexts/ThemeContext';
import { useSphereNavigation } from '@/contexts/NavigationContext';

// ═══════════════════════════════════════════════════════════════════
// TYPES
// ═══════════════════════════════════════════════════════════════════

interface CrossSphereImportPanelProps {
  isOpen: boolean;
  onClose: () => void;
  currentContext?: {
    projectId?: string;
    projectName?: string;
  };
}

interface SearchResult {
  item_id: string;
  item_type: string;
  title: string;
  description?: string;
  sphere_id: string;
  created_at: string;
  preview?: any;
  already_imported: boolean;
}

interface ImportLocation {
  id: string;
  type: string;
  path: string;
  name: string;
}

// ═══════════════════════════════════════════════════════════════════
// SPHERE DATA
// ═══════════════════════════════════════════════════════════════════

const SPHERES = [
  { id: 'personal', name: 'Personnel', emoji: '🏠', symbol: '◇', color: '#3EB4A2' },
  { id: 'business', name: 'Affaires', emoji: '💼', symbol: '⬡', color: '#5BA9FF' },
  { id: 'government', name: 'Gouvernement', emoji: '🏛️', symbol: '⏣', color: '#9B8FD0' },
  { id: 'creative', name: 'Créatif', emoji: '🎨', symbol: '✦', color: '#FF8BAA' },
  { id: 'community', name: 'Communauté', emoji: '👥', symbol: '◉', color: '#22C55E' },
  { id: 'social', name: 'Social', emoji: '📱', symbol: '⊛', color: '#1DA1F2' },
  { id: 'entertainment', name: 'Divertissement', emoji: '🎬', symbol: '▷', color: '#F39C12' },
  { id: 'team', name: 'Mon Équipe', emoji: '🤝', symbol: '⎔', color: '#8B5CF6' }
];

// ═══════════════════════════════════════════════════════════════════
// MAIN COMPONENT
// ═══════════════════════════════════════════════════════════════════

export const CrossSphereImportPanel: React.FC<CrossSphereImportPanelProps> = ({
  isOpen,
  onClose,
  currentContext
}) => {
  const { currentSphere } = useSphereNavigation();
  const { resolvedTheme } = useTheme();
  
  // State
  const [step, setStep] = useState<'select-sphere' | 'browse' | 'select-location' | 'confirm'>('select-sphere');
  const [selectedSourceSphere, setSelectedSourceSphere] = useState<string | null>(null);
  const [searchQuery, setSearchQuery] = useState('');
  const [searchResults, setSearchResults] = useState<SearchResult[]>([]);
  const [selectedItems, setSelectedItems] = useState<SearchResult[]>([]);
  const [importLocation, setImportLocation] = useState<ImportLocation | null>(null);
  const [isLoading, setIsLoading] = useState(false);
  
  // Available spheres (exclude current)
  const availableSpheres = SPHERES.filter(s => s.id !== currentSphere);
  
  // Current sphere data
  const currentSphereData = SPHERES.find(s => s.id === currentSphere);
  
  // Reset state when panel opens/closes
  useEffect(() => {
    if (!isOpen) {
      setTimeout(() => {
        setStep('select-sphere');
        setSelectedSourceSphere(null);
        setSearchQuery('');
        setSearchResults([]);
        setSelectedItems([]);
        setImportLocation(null);
      }, 300);
    }
  }, [isOpen]);
  
  // Search function
  const handleSearch = useCallback(async () => {
    if (!selectedSourceSphere || !searchQuery.trim()) return;
    
    setIsLoading(true);
    try {
      const response = await fetch(
        `/api/cross-sphere/search?query=${encodeURIComponent(searchQuery)}&source_spheres=${selectedSourceSphere}`
      );
      const data = await response.json();
      setSearchResults(data.results);
    } catch (error) {
      console.error('Search error:', error);
    } finally {
      setIsLoading(false);
    }
  }, [selectedSourceSphere, searchQuery]);
  
  // Toggle item selection
  const toggleItemSelection = (item: SearchResult) => {
    if (item.already_imported) return;
    
    setSelectedItems(prev => {
      const exists = prev.find(i => i.item_id === item.item_id);
      if (exists) {
        return prev.filter(i => i.item_id !== item.item_id);
      }
      return [...prev, item];
    });
  };
  
  // Perform import
  const handleImport = async () => {
    if (!importLocation || selectedItems.length === 0) return;
    
    setIsLoading(true);
    try {
      for (const item of selectedItems) {
        await fetch('/api/cross-sphere/import', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            source_sphere_id: item.sphere_id,
            source_item_type: item.item_type,
            source_item_id: item.item_id,
            target_location: importLocation,
            notify_on_change: true
          })
        });
      }
      
      onClose();
      // Show success toast
    } catch (error) {
      console.error('Import error:', error);
    } finally {
      setIsLoading(false);
    }
  };
  
  return (
    <AnimatePresence>
      {isOpen && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 backdrop-blur-sm"
          onClick={onClose}
        >
          <motion.div
            initial={{ scale: 0.95, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            exit={{ scale: 0.95, opacity: 0 }}
            className="bg-surface rounded-xl shadow-2xl w-full max-w-4xl max-h-[80vh] overflow-hidden"
            onClick={e => e.stopPropagation()}
          >
            {/* Header */}
            <div className="p-4 border-b border-white/10 flex items-center justify-between">
              <div className="flex items-center gap-3">
                <span className="text-2xl">🔀</span>
                <div>
                  <h2 className="text-lg font-semibold text-white">
                    Importer depuis une autre sphère
                  </h2>
                  <p className="text-sm text-white/60">
                    Sphère active: {currentSphereData?.symbol} {currentSphereData?.name}
                  </p>
                </div>
              </div>
              <button onClick={onClose} className="text-white/60 hover:text-white">
                ✕
              </button>
            </div>
            
            {/* Progress Steps */}
            <div className="px-4 py-2 border-b border-white/10">
              <div className="flex items-center gap-2">
                {['select-sphere', 'browse', 'select-location', 'confirm'].map((s, i) => (
                  <React.Fragment key={s}>
                    <div className={`
                      w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium
                      ${step === s 
                        ? 'bg-primary text-white' 
                        : i < ['select-sphere', 'browse', 'select-location', 'confirm'].indexOf(step)
                          ? 'bg-green-500 text-white'
                          : 'bg-white/10 text-white/40'
                      }
                    `}>
                      {i + 1}
                    </div>
                    {i < 3 && <div className="flex-1 h-0.5 bg-white/10" />}
                  </React.Fragment>
                ))}
              </div>
              <div className="flex justify-between mt-1 text-xs text-white/40">
                <span>Sphère</span>
                <span>Rechercher</span>
                <span>Emplacement</span>
                <span>Confirmer</span>
              </div>
            </div>
            
            {/* Content */}
            <div className="p-4 overflow-y-auto" style={{ maxHeight: 'calc(80vh - 200px)' }}>
              {/* Step 1: Select Source Sphere */}
              {step === 'select-sphere' && (
                <div className="space-y-4">
                  <p className="text-white/80">
                    Choisissez la sphère depuis laquelle importer:
                  </p>
                  <div className="grid grid-cols-4 gap-3">
                    {availableSpheres.map(sphere => (
                      <button
                        key={sphere.id}
                        onClick={() => {
                          setSelectedSourceSphere(sphere.id);
                          setStep('browse');
                        }}
                        className={`
                          p-4 rounded-lg border-2 transition-all
                          hover:border-opacity-100 hover:bg-white/5
                          ${selectedSourceSphere === sphere.id 
                            ? 'border-opacity-100' 
                            : 'border-opacity-20'
                          }
                        `}
                        style={{ borderColor: sphere.color }}
                      >
                        <div className="text-3xl mb-2">{sphere.symbol}</div>
                        <div className="text-white font-medium">{sphere.name}</div>
                        <div className="text-white/40 text-sm">{sphere.emoji}</div>
                      </button>
                    ))}
                  </div>
                </div>
              )}
              
              {/* Step 2: Browse & Search */}
              {step === 'browse' && selectedSourceSphere && (
                <div className="space-y-4">
                  {/* Source sphere indicator */}
                  <div 
                    className="flex items-center gap-2 p-3 rounded-lg"
                    style={{ backgroundColor: `${SPHERES.find(s => s.id === selectedSourceSphere)?.color}22` }}
                  >
                    <span className="text-xl">
                      {SPHERES.find(s => s.id === selectedSourceSphere)?.symbol}
                    </span>
                    <span className="text-white font-medium">
                      {SPHERES.find(s => s.id === selectedSourceSphere)?.name}
                    </span>
                    <button 
                      onClick={() => setStep('select-sphere')}
                      className="ml-auto text-white/60 hover:text-white text-sm"
                    >
                      Changer
                    </button>
                  </div>
                  
                  {/* Search bar */}
                  <div className="flex gap-2">
                    <input
                      type="text"
                      value={searchQuery}
                      onChange={e => setSearchQuery(e.target.value)}
                      onKeyDown={e => e.key === 'Enter' && handleSearch()}
                      placeholder="Rechercher threads, notes, documents..."
                      className="flex-1 px-4 py-2 bg-white/10 rounded-lg text-white placeholder-white/40 focus:outline-none focus:ring-2 focus:ring-primary"
                    />
                    <button
                      onClick={handleSearch}
                      disabled={isLoading}
                      className="px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary/80 disabled:opacity-50"
                    >
                      {isLoading ? '...' : '🔍 Rechercher'}
                    </button>
                  </div>
                  
                  {/* Results */}
                  <div className="space-y-2">
                    {searchResults.length === 0 && searchQuery && !isLoading && (
                      <div className="text-center py-8 text-white/40">
                        Aucun résultat pour "{searchQuery}"
                      </div>
                    )}
                    
                    {searchResults.map(item => (
                      <div
                        key={item.item_id}
                        onClick={() => toggleItemSelection(item)}
                        className={`
                          p-3 rounded-lg border-2 cursor-pointer transition-all
                          ${item.already_imported 
                            ? 'opacity-50 cursor-not-allowed border-white/10' 
                            : selectedItems.find(i => i.item_id === item.item_id)
                              ? 'border-primary bg-primary/10'
                              : 'border-white/10 hover:border-white/30'
                          }
                        `}
                      >
                        <div className="flex items-start gap-3">
                          <input
                            type="checkbox"
                            checked={selectedItems.some(i => i.item_id === item.item_id)}
                            disabled={item.already_imported}
                            readOnly
                            className="mt-1"
                          />
                          <div className="flex-1">
                            <div className="flex items-center gap-2">
                              <span className="text-lg">
                                {item.item_type === 'thread' ? '📄' :
                                 item.item_type === 'note' ? '📝' :
                                 item.item_type === 'document' ? '📁' :
                                 item.item_type === 'task' ? '✅' : '📦'}
                              </span>
                              <span className="text-white font-medium">{item.title}</span>
                              {item.already_imported && (
                                <span className="text-xs bg-yellow-500/20 text-yellow-400 px-2 py-0.5 rounded">
                                  Déjà importé
                                </span>
                              )}
                            </div>
                            {item.description && (
                              <p className="text-white/60 text-sm mt-1">{item.description}</p>
                            )}
                            <div className="text-white/40 text-xs mt-1">
                              {item.item_type} • {new Date(item.created_at).toLocaleDateString()}
                            </div>
                          </div>
                        </div>
                      </div>
                    ))}
                  </div>
                  
                  {/* Selection summary */}
                  {selectedItems.length > 0 && (
                    <div className="flex items-center justify-between p-3 bg-primary/10 rounded-lg">
                      <span className="text-white">
                        {selectedItems.length} élément(s) sélectionné(s)
                      </span>
                      <button
                        onClick={() => setStep('select-location')}
                        className="px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary/80"
                      >
                        Choisir l'emplacement →
                      </button>
                    </div>
                  )}
                </div>
              )}
              
              {/* Step 3: Select Import Location */}
              {step === 'select-location' && (
                <ImportLocationSelector
                  sphereId={currentSphere}
                  currentContext={currentContext}
                  onSelect={(location) => {
                    setImportLocation(location);
                    setStep('confirm');
                  }}
                  onBack={() => setStep('browse')}
                />
              )}
              
              {/* Step 4: Confirm */}
              {step === 'confirm' && importLocation && (
                <div className="space-y-4">
                  <h3 className="text-lg font-semibold text-white">
                    Confirmer l'importation
                  </h3>
                  
                  {/* Summary */}
                  <div className="p-4 bg-white/5 rounded-lg space-y-4">
                    <div>
                      <div className="text-white/60 text-sm">Source:</div>
                      <div className="text-white flex items-center gap-2">
                        <span>{SPHERES.find(s => s.id === selectedSourceSphere)?.symbol}</span>
                        <span>{SPHERES.find(s => s.id === selectedSourceSphere)?.name}</span>
                      </div>
                    </div>
                    
                    <div>
                      <div className="text-white/60 text-sm">Éléments à importer:</div>
                      <div className="space-y-1 mt-1">
                        {selectedItems.map(item => (
                          <div key={item.item_id} className="text-white flex items-center gap-2">
                            <span>📄</span>
                            <span>{item.title}</span>
                          </div>
                        ))}
                      </div>
                    </div>
                    
                    <div>
                      <div className="text-white/60 text-sm">Destination:</div>
                      <div className="text-white flex items-center gap-2">
                        <span>{currentSphereData?.symbol}</span>
                        <span>{importLocation.path}</span>
                      </div>
                    </div>
                  </div>
                  
                  {/* Options */}
                  <div className="space-y-2">
                    <label className="flex items-center gap-2 text-white/80">
                      <input type="checkbox" defaultChecked className="rounded" />
                      <span>Notifier quand l'original est modifié</span>
                    </label>
                  </div>
                  
                  {/* Info box */}
                  <div className="p-3 bg-blue-500/10 border border-blue-500/20 rounded-lg text-blue-300 text-sm">
                    ℹ️ Une <strong>référence</strong> sera créée. L'original reste dans sa sphère source et peut être mis à jour indépendamment.
                  </div>
                  
                  {/* Actions */}
                  <div className="flex gap-3">
                    <button
                      onClick={() => setStep('select-location')}
                      className="px-4 py-2 bg-white/10 text-white rounded-lg hover:bg-white/20"
                    >
                      ← Retour
                    </button>
                    <button
                      onClick={handleImport}
                      disabled={isLoading}
                      className="flex-1 px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary/80 disabled:opacity-50"
                    >
                      {isLoading ? 'Importation...' : '✓ Confirmer l\'importation'}
                    </button>
                  </div>
                </div>
              )}
            </div>
          </motion.div>
        </motion.div>
      )}
    </AnimatePresence>
  );
};

// ═══════════════════════════════════════════════════════════════════
// IMPORT LOCATION SELECTOR
// ═══════════════════════════════════════════════════════════════════

interface ImportLocationSelectorProps {
  sphereId: string;
  currentContext?: { projectId?: string; projectName?: string };
  onSelect: (location: ImportLocation) => void;
  onBack: () => void;
}

const ImportLocationSelector: React.FC<ImportLocationSelectorProps> = ({
  sphereId,
  currentContext,
  onSelect,
  onBack
}) => {
  const [locations, setLocations] = useState<ImportLocation[]>([]);
  const [favorites, setFavorites] = useState<ImportLocation[]>([]);
  const [currentPath, setCurrentPath] = useState<string[]>([]);
  const [newFolderName, setNewFolderName] = useState('');
  const [showNewFolder, setShowNewFolder] = useState(false);
  
  useEffect(() => {
    // Fetch locations
    fetchLocations();
  }, [sphereId, currentPath]);
  
  const fetchLocations = async () => {
    // API call to get locations
  };
  
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h3 className="text-lg font-semibold text-white">
          Choisir l'emplacement d'import
        </h3>
        <button onClick={onBack} className="text-white/60 hover:text-white text-sm">
          ← Retour
        </button>
      </div>
      
      {/* Quick access - Current context */}
      {currentContext?.projectId && (
        <div className="p-3 bg-primary/10 rounded-lg">
          <div className="text-white/60 text-sm mb-2">Contexte actuel:</div>
          <button
            onClick={() => onSelect({
              id: currentContext.projectId!,
              type: 'project',
              path: currentContext.projectName!,
              name: currentContext.projectName!
            })}
            className="w-full p-2 bg-white/10 rounded flex items-center gap-2 text-white hover:bg-white/20"
          >
            <span>📁</span>
            <span>{currentContext.projectName}</span>
            <span className="ml-auto text-white/40">Importer ici</span>
          </button>
        </div>
      )}
      
      {/* Favorites */}
      {favorites.length > 0 && (
        <div>
          <div className="text-white/60 text-sm mb-2">⭐ Favoris:</div>
          <div className="space-y-1">
            {favorites.map(loc => (
              <button
                key={loc.id}
                onClick={() => onSelect(loc)}
                className="w-full p-2 bg-white/5 rounded flex items-center gap-2 text-white hover:bg-white/10"
              >
                <span>📁</span>
                <span>{loc.name}</span>
              </button>
            ))}
          </div>
        </div>
      )}
      
      {/* Browse structure */}
      <div>
        <div className="text-white/60 text-sm mb-2">Parcourir:</div>
        
        {/* Breadcrumb */}
        <div className="flex items-center gap-1 text-sm text-white/60 mb-2">
          <button onClick={() => setCurrentPath([])} className="hover:text-white">
            🏠 Racine
          </button>
          {currentPath.map((p, i) => (
            <React.Fragment key={i}>
              <span>/</span>
              <button 
                onClick={() => setCurrentPath(currentPath.slice(0, i + 1))}
                className="hover:text-white"
              >
                {p}
              </button>
            </React.Fragment>
          ))}
        </div>
        
        {/* Locations list */}
        <div className="space-y-1 max-h-48 overflow-y-auto">
          {locations.map(loc => (
            <div
              key={loc.id}
              className="flex items-center gap-2 p-2 bg-white/5 rounded hover:bg-white/10"
            >
              <span>📁</span>
              <span className="text-white flex-1">{loc.name}</span>
              <button
                onClick={() => onSelect(loc)}
                className="px-2 py-1 bg-primary/20 text-primary text-sm rounded hover:bg-primary/30"
              >
                Sélectionner
              </button>
            </div>
          ))}
        </div>
        
        {/* Create new folder */}
        {showNewFolder ? (
          <div className="flex gap-2 mt-2">
            <input
              type="text"
              value={newFolderName}
              onChange={e => setNewFolderName(e.target.value)}
              placeholder="Nom du nouveau dossier"
              className="flex-1 px-3 py-2 bg-white/10 rounded text-white placeholder-white/40"
            />
            <button
              onClick={() => {
                // Create folder and select it
                setShowNewFolder(false);
              }}
              className="px-3 py-2 bg-primary text-white rounded"
            >
              Créer
            </button>
            <button
              onClick={() => setShowNewFolder(false)}
              className="px-3 py-2 bg-white/10 text-white rounded"
            >
              Annuler
            </button>
          </div>
        ) : (
          <button
            onClick={() => setShowNewFolder(true)}
            className="w-full mt-2 p-2 border-2 border-dashed border-white/20 rounded text-white/60 hover:border-white/40 hover:text-white/80"
          >
            + Créer un nouveau dossier
          </button>
        )}
      </div>
    </div>
  );
};

export default CrossSphereImportPanel;
```

---

# 🔗 REFERENCE BADGE COMPONENT

```tsx
// components/cross-sphere/CrossSphereReferenceBadge.tsx

import React from 'react';
import { motion } from 'framer-motion';

interface CrossSphereReferenceBadgeProps {
  sourceSpehereId: string;
  sourceSphereSymbol: string;
  sourceSphereColor: string;
  sourceTitle: string;
  syncStatus: 'synced' | 'pending' | 'conflict';
  lastSyncAt?: string;
  onOpenSource: () => void;
  onSync: () => void;
  onRemove: () => void;
}

export const CrossSphereReferenceBadge: React.FC<CrossSphereReferenceBadgeProps> = ({
  sourceSpehereId,
  sourceSphereSymbol,
  sourceSphereColor,
  sourceTitle,
  syncStatus,
  lastSyncAt,
  onOpenSource,
  onSync,
  onRemove
}) => {
  const statusColors = {
    synced: '#22C55E',
    pending: '#F59E0B',
    conflict: '#EF4444'
  };
  
  const statusLabels = {
    synced: 'Synchronisé',
    pending: 'En attente',
    conflict: 'Conflit'
  };
  
  return (
    <div 
      className="p-3 rounded-lg border"
      style={{ 
        backgroundColor: `${sourceSphereColor}11`,
        borderColor: `${sourceSphereColor}33`
      }}
    >
      {/* Header */}
      <div className="flex items-center gap-2 mb-2">
        <span className="text-lg" style={{ color: sourceSphereColor }}>
          🔗
        </span>
        <span className="text-white/60 text-sm">Référence depuis</span>
        <span style={{ color: sourceSphereColor }}>{sourceSphereSymbol}</span>
      </div>
      
      {/* Title */}
      <div className="text-white font-medium mb-2">{sourceTitle}</div>
      
      {/* Status */}
      <div className="flex items-center gap-2 mb-3">
        <div 
          className="w-2 h-2 rounded-full"
          style={{ backgroundColor: statusColors[syncStatus] }}
        />
        <span className="text-sm" style={{ color: statusColors[syncStatus] }}>
          {statusLabels[syncStatus]}
        </span>
        {lastSyncAt && (
          <span className="text-white/40 text-xs">
            • Dernière sync: {new Date(lastSyncAt).toLocaleString()}
          </span>
        )}
      </div>
      
      {/* Actions */}
      <div className="flex gap-2">
        <button
          onClick={onOpenSource}
          className="flex-1 px-3 py-1.5 bg-white/10 rounded text-white text-sm hover:bg-white/20"
        >
          Ouvrir l'original
        </button>
        <button
          onClick={onSync}
          className="px-3 py-1.5 bg-white/10 rounded text-white text-sm hover:bg-white/20"
        >
          🔄
        </button>
        <button
          onClick={onRemove}
          className="px-3 py-1.5 bg-red-500/10 rounded text-red-400 text-sm hover:bg-red-500/20"
        >
          ✕
        </button>
      </div>
    </div>
  );
};
```

---

# 📊 RÉSUMÉ DU SYSTÈME

```
╔═══════════════════════════════════════════════════════════════════════════════╗
║                    CROSS-SPHERE IMPORT SYSTEM                                 ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║                                                                               ║
║   ✅ SÉPARATION MAINTENUE                                                    ║
║   • Les données restent dans leur sphère d'origine                           ║
║   • Seules des RÉFÉRENCES sont créées                                        ║
║   • Pas de duplication, pas de mélange                                       ║
║                                                                               ║
║   ✅ NAVIGATION CROSS-SPHERE                                                 ║
║   • Parcourir d'autres sphères sans quitter l'active                         ║
║   • Recherche multi-sphères                                                  ║
║   • Aperçu du contenu avant import                                           ║
║                                                                               ║
║   ✅ EMPLACEMENT D'IMPORT OBLIGATOIRE                                        ║
║   • L'utilisateur DOIT choisir où placer la référence                        ║
║   • Structure de dossiers/projets                                            ║
║   • Favoris pour accès rapide                                                ║
║                                                                               ║
║   ✅ TRAÇABILITÉ COMPLÈTE                                                    ║
║   • Source clairement identifiée                                             ║
║   • Historique des synchronisations                                          ║
║   • Notifications de changements                                             ║
║                                                                               ║
║   ✅ TYPES DE RÉFÉRENCES                                                     ║
║   • Link: Lien simple vers l'original                                        ║
║   • Mirror: Synchronisation bidirectionnelle                                 ║
║   • Snapshot: Copie figée à un instant T                                     ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝
```

---

*CHE·NU™ Cross-Sphere Import System v1.0*
*Séparation avec Ponts Intelligents*
