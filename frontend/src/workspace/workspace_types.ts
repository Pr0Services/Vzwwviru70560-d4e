/**
 * CHE·NU™ — Workspace Types (Legacy Stub)
 */
export interface WorkspaceConfig {
  id: string;
  name: string;
  layout: string;
}
export interface WorkspaceState {
  active: boolean;
  lastAccess: string;
}
