/**
 * ╔══════════════════════════════════════════════════════════════════════════════╗
 * ║                    CHE·NU™ — DEMO PAGE                                       ║
 * ║                    Démonstration interactive                                 ║
 * ╚══════════════════════════════════════════════════════════════════════════════╝
 */

import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { 
  Play, Pause, ChevronRight, ChevronLeft, Monitor, 
  Smartphone, Brain, Shield, Users, Zap, 
  Calendar, FileText, MessageSquare, Settings,
  Home, Briefcase, Palette, GraduationCap, Building2
} from 'lucide-react';

// Demo Steps
interface DemoStep {
  id: string;
  title: string;
  description: string;
  icon: React.ReactNode;
  features: string[];
}

const demoSteps: DemoStep[] = [
  {
    id: 'spheres',
    title: 'Les 9 Sphères de Vie',
    description: 'Organisez votre vie en contextes séparés. Chaque sphère isole vos données et agents.',
    icon: <Brain className="w-8 h-8" />,
    features: [
      'Personnel, Business, Créatif...',
      'Données isolées par sphère',
      'Agents dédiés par contexte',
    ],
  },
  {
    id: 'nova',
    title: 'Nova, votre Intelligence Système',
    description: 'Nova supervise tout, orchestre vos agents, et vous guide dans CHE·NU.',
    icon: <Zap className="w-8 h-8" />,
    features: [
      'Toujours présente',
      'Recommandations intelligentes',
      '226 agents sous sa supervision',
    ],
  },
  {
    id: 'governance',
    title: 'Gouvernance > Exécution',
    description: 'Chaque action importante passe par votre approbation. Vous gardez le contrôle.',
    icon: <Shield className="w-8 h-8" />,
    features: [
      'Checkpoints de validation',
      'Historique auditable',
      'Contrôle des coûts en temps réel',
    ],
  },
  {
    id: 'bureau',
    title: 'Le Bureau Unifié',
    description: 'Chaque sphère ouvre le même Bureau avec: Notes, Tâches, Projets, Threads, Meetings.',
    icon: <FileText className="w-8 h-8" />,
    features: [
      'Structure cohérente',
      'Threads persistants',
      'Meetings AI intégrés',
    ],
  },
  {
    id: 'agents',
    title: '226 Agents Spécialisés',
    description: 'Des experts IA pour chaque domaine: juridique, comptable, créatif, technique...',
    icon: <Users className="w-8 h-8" />,
    features: [
      'Agents par domaine',
      'Coûts transparents',
      'Création d\'agents custom',
    ],
  },
];

// Sphere Icons
const sphereIcons = [
  { icon: <Home className="w-6 h-6" />, name: 'Personal', color: '#3EB4A2' },
  { icon: <Briefcase className="w-6 h-6" />, name: 'Business', color: '#D8B26A' },
  { icon: <Building2 className="w-6 h-6" />, name: 'Government', color: '#8D8371' },
  { icon: <Palette className="w-6 h-6" />, name: 'Studio', color: '#7A593A' },
  { icon: <Users className="w-6 h-6" />, name: 'Community', color: '#3F7249' },
  { icon: <MessageSquare className="w-6 h-6" />, name: 'Social', color: '#3EB4A2' },
  { icon: <Play className="w-6 h-6" />, name: 'Entertainment', color: '#D8B26A' },
  { icon: <Users className="w-6 h-6" />, name: 'My Team', color: '#8D8371' },
  { icon: <GraduationCap className="w-6 h-6" />, name: 'Scholar', color: '#3F7249' },
];

// Main Component
export const DemoPage: React.FC = () => {
  const [currentStep, setCurrentStep] = useState(0);
  const [isPlaying, setIsPlaying] = useState(false);
  const [viewMode, setViewMode] = useState<'desktop' | 'mobile'>('desktop');
  
  const nextStep = () => {
    setCurrentStep(prev => (prev + 1) % demoSteps.length);
  };
  
  const prevStep = () => {
    setCurrentStep(prev => (prev - 1 + demoSteps.length) % demoSteps.length);
  };
  
  return (
    <div className="min-h-screen bg-gradient-to-b from-[#1E1F22] to-[#2F4C39]">
      {/* Navigation */}
      <nav className="fixed top-0 left-0 right-0 z-50 bg-[#1E1F22]/90 backdrop-blur-md border-b border-[#D8B26A]/20">
        <div className="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
          <Link to="/" className="flex items-center gap-3">
            <div className="w-10 h-10 rounded-lg bg-gradient-to-br from-[#D8B26A] to-[#7A593A] flex items-center justify-center">
              <span className="text-white font-bold text-lg">C</span>
            </div>
            <span className="text-2xl font-bold text-white">
              CHE<span className="text-[#D8B26A]">·</span>NU
            </span>
          </Link>
          
          <div className="flex items-center gap-4">
            <Link to="/pricing" className="text-[#E9E4D6] hover:text-[#D8B26A] transition">
              Tarifs
            </Link>
            <Link to="/signup" className="px-6 py-2 bg-[#D8B26A] text-[#1E1F22] font-semibold rounded-lg hover:bg-[#E9E4D6] transition">
              Essayer gratuitement
            </Link>
          </div>
        </div>
      </nav>

      {/* Header */}
      <section className="pt-32 pb-8 px-6">
        <div className="max-w-4xl mx-auto text-center">
          <div className="inline-flex items-center gap-2 px-4 py-2 bg-[#D8B26A]/20 rounded-full mb-6">
            <Play className="w-4 h-4 text-[#D8B26A]" />
            <span className="text-[#D8B26A] text-sm font-medium">
              Démonstration interactive
            </span>
          </div>
          
          <h1 className="text-5xl font-bold text-white mb-6">
            Découvrez CHE·NU en action
          </h1>
          <p className="text-xl text-[#E9E4D6]/70">
            Explorez les fonctionnalités clés de votre futur système d'intelligence gouvernée
          </p>
        </div>
      </section>

      {/* Demo Viewer */}
      <section className="py-8 px-6">
        <div className="max-w-6xl mx-auto">
          {/* View Mode Toggle */}
          <div className="flex justify-center gap-2 mb-6">
            <button
              onClick={() => setViewMode('desktop')}
              className={`p-3 rounded-lg transition ${
                viewMode === 'desktop' 
                  ? 'bg-[#D8B26A] text-[#1E1F22]' 
                  : 'bg-[#1E1F22] text-[#E9E4D6]/60 hover:text-white'
              }`}
            >
              <Monitor className="w-5 h-5" />
            </button>
            <button
              onClick={() => setViewMode('mobile')}
              className={`p-3 rounded-lg transition ${
                viewMode === 'mobile' 
                  ? 'bg-[#D8B26A] text-[#1E1F22]' 
                  : 'bg-[#1E1F22] text-[#E9E4D6]/60 hover:text-white'
              }`}
            >
              <Smartphone className="w-5 h-5" />
            </button>
          </div>
          
          {/* Demo Frame */}
          <div className={`
            mx-auto bg-[#1E1F22] rounded-2xl border-4 border-[#8D8371]/30 overflow-hidden shadow-2xl
            ${viewMode === 'desktop' ? 'max-w-5xl aspect-video' : 'max-w-sm aspect-[9/16]'}
          `}>
            {/* Browser Bar */}
            <div className="bg-[#2F4C39]/50 px-4 py-3 flex items-center gap-3 border-b border-[#8D8371]/20">
              <div className="flex gap-2">
                <div className="w-3 h-3 rounded-full bg-red-500/70" />
                <div className="w-3 h-3 rounded-full bg-yellow-500/70" />
                <div className="w-3 h-3 rounded-full bg-green-500/70" />
              </div>
              <div className="flex-1 bg-[#1E1F22] rounded-lg px-4 py-1.5 text-[#8D8371] text-sm">
                app.chenu.ai
              </div>
            </div>
            
            {/* Demo Content */}
            <div className="p-6 h-full bg-gradient-to-br from-[#1E1F22] to-[#2F4C39]/30">
              {/* Simulated Interface based on current step */}
              {currentStep === 0 && (
                <div className="h-full flex flex-col">
                  <h3 className="text-[#D8B26A] font-bold mb-4">Sélectionnez une Sphère</h3>
                  <div className="grid grid-cols-3 gap-4 flex-1">
                    {sphereIcons.map((sphere, idx) => (
                      <div 
                        key={idx}
                        className="p-4 bg-[#1E1F22] rounded-xl border border-[#8D8371]/20 hover:border-[#D8B26A] transition cursor-pointer flex flex-col items-center justify-center"
                        style={{ borderColor: idx === 0 ? sphere.color : undefined }}
                      >
                        <div style={{ color: sphere.color }}>{sphere.icon}</div>
                        <span className="text-white text-sm mt-2">{sphere.name}</span>
                      </div>
                    ))}
                  </div>
                </div>
              )}
              
              {currentStep === 1 && (
                <div className="h-full flex flex-col items-center justify-center text-center">
                  <div className="w-24 h-24 rounded-full bg-gradient-to-br from-[#D8B26A] to-[#3EB4A2] flex items-center justify-center mb-6 animate-pulse">
                    <Brain className="w-12 h-12 text-white" />
                  </div>
                  <h3 className="text-2xl font-bold text-white mb-2">Nova</h3>
                  <p className="text-[#E9E4D6]/60 mb-6">Intelligence Système CHE·NU</p>
                  <div className="bg-[#1E1F22] rounded-xl p-4 max-w-md">
                    <p className="text-[#E9E4D6]">
                      "Bonjour! Je suis Nova. Comment puis-je vous aider aujourd'hui?"
                    </p>
                  </div>
                </div>
              )}
              
              {currentStep === 2 && (
                <div className="h-full flex flex-col">
                  <div className="bg-[#D8B26A]/20 border border-[#D8B26A] rounded-xl p-6 mb-4">
                    <div className="flex items-center gap-3 mb-4">
                      <Shield className="w-6 h-6 text-[#D8B26A]" />
                      <span className="text-[#D8B26A] font-bold">Checkpoint de Gouvernance</span>
                    </div>
                    <p className="text-white mb-4">
                      L'agent Comptable veut accéder à vos documents financiers pour générer le rapport Q4.
                    </p>
                    <div className="flex gap-3">
                      <button className="px-6 py-2 bg-[#3EB4A2] text-white rounded-lg font-semibold">
                        ✓ Approuver
                      </button>
                      <button className="px-6 py-2 bg-[#7A593A] text-white rounded-lg font-semibold">
                        ✗ Refuser
                      </button>
                    </div>
                  </div>
                  <div className="text-[#E9E4D6]/60 text-sm">
                    <p>Coût estimé: 45 tokens (~$0.02)</p>
                    <p>Temps estimé: 30 secondes</p>
                  </div>
                </div>
              )}
              
              {currentStep === 3 && (
                <div className="h-full flex">
                  {/* Sidebar */}
                  <div className="w-48 bg-[#1E1F22] rounded-xl p-4 mr-4">
                    <h4 className="text-[#D8B26A] font-bold text-sm mb-4">Bureau</h4>
                    <nav className="space-y-2">
                      {['Dashboard', 'Notes', 'Tâches', 'Projets', 'Threads', 'Meetings'].map((item, idx) => (
                        <div 
                          key={idx}
                          className={`px-3 py-2 rounded-lg text-sm cursor-pointer ${
                            idx === 0 
                              ? 'bg-[#D8B26A]/20 text-[#D8B26A]' 
                              : 'text-[#E9E4D6]/60 hover:text-white'
                          }`}
                        >
                          {item}
                        </div>
                      ))}
                    </nav>
                  </div>
                  {/* Content */}
                  <div className="flex-1 bg-[#1E1F22] rounded-xl p-4">
                    <h4 className="text-white font-bold mb-4">Dashboard - Business</h4>
                    <div className="grid grid-cols-2 gap-4">
                      <div className="bg-[#2F4C39]/30 rounded-lg p-4">
                        <p className="text-[#E9E4D6]/60 text-sm">Threads actifs</p>
                        <p className="text-2xl font-bold text-white">12</p>
                      </div>
                      <div className="bg-[#2F4C39]/30 rounded-lg p-4">
                        <p className="text-[#E9E4D6]/60 text-sm">Tâches en cours</p>
                        <p className="text-2xl font-bold text-white">8</p>
                      </div>
                    </div>
                  </div>
                </div>
              )}
              
              {currentStep === 4 && (
                <div className="h-full">
                  <h3 className="text-[#D8B26A] font-bold mb-4">Agents Disponibles</h3>
                  <div className="space-y-3">
                    {[
                      { name: 'Agent Juridique', specialty: 'Contrats, conformité', cost: '12 tokens/req' },
                      { name: 'Agent Comptable', specialty: 'Finances, rapports', cost: '15 tokens/req' },
                      { name: 'Agent Rédacteur', specialty: 'Contenu, copywriting', cost: '8 tokens/req' },
                      { name: 'Agent Développeur', specialty: 'Code, technique', cost: '20 tokens/req' },
                    ].map((agent, idx) => (
                      <div 
                        key={idx}
                        className="flex items-center justify-between p-4 bg-[#1E1F22] rounded-xl border border-[#8D8371]/20"
                      >
                        <div className="flex items-center gap-3">
                          <div className="w-10 h-10 rounded-lg bg-[#3EB4A2]/20 flex items-center justify-center">
                            <Users className="w-5 h-5 text-[#3EB4A2]" />
                          </div>
                          <div>
                            <p className="text-white font-medium">{agent.name}</p>
                            <p className="text-[#E9E4D6]/50 text-sm">{agent.specialty}</p>
                          </div>
                        </div>
                        <span className="text-[#D8B26A] text-sm">{agent.cost}</span>
                      </div>
                    ))}
                  </div>
                </div>
              )}
            </div>
          </div>
          
          {/* Controls */}
          <div className="flex items-center justify-center gap-4 mt-8">
            <button
              onClick={prevStep}
              className="p-3 bg-[#1E1F22] border border-[#8D8371]/30 rounded-xl hover:border-[#D8B26A] transition"
            >
              <ChevronLeft className="w-6 h-6 text-[#E9E4D6]" />
            </button>
            
            <div className="flex gap-2">
              {demoSteps.map((_, idx) => (
                <button
                  key={idx}
                  onClick={() => setCurrentStep(idx)}
                  className={`w-3 h-3 rounded-full transition ${
                    idx === currentStep ? 'bg-[#D8B26A]' : 'bg-[#8D8371]/30 hover:bg-[#8D8371]'
                  }`}
                />
              ))}
            </div>
            
            <button
              onClick={nextStep}
              className="p-3 bg-[#1E1F22] border border-[#8D8371]/30 rounded-xl hover:border-[#D8B26A] transition"
            >
              <ChevronRight className="w-6 h-6 text-[#E9E4D6]" />
            </button>
          </div>
        </div>
      </section>

      {/* Step Info */}
      <section className="py-12 px-6">
        <div className="max-w-4xl mx-auto">
          <div className="bg-[#1E1F22] rounded-2xl p-8 border border-[#D8B26A]/20">
            <div className="flex items-start gap-6">
              <div className="w-16 h-16 bg-[#D8B26A]/20 rounded-xl flex items-center justify-center shrink-0 text-[#D8B26A]">
                {demoSteps[currentStep].icon}
              </div>
              <div>
                <h2 className="text-2xl font-bold text-white mb-2">
                  {demoSteps[currentStep].title}
                </h2>
                <p className="text-[#E9E4D6]/70 mb-4">
                  {demoSteps[currentStep].description}
                </p>
                <ul className="space-y-2">
                  {demoSteps[currentStep].features.map((feature, idx) => (
                    <li key={idx} className="flex items-center gap-2 text-[#E9E4D6]">
                      <ChevronRight className="w-4 h-4 text-[#3EB4A2]" />
                      {feature}
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* CTA */}
      <section className="py-16 px-6 bg-[#1E1F22]">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-4xl font-bold text-white mb-6">
            Prêt à essayer CHE·NU?
          </h2>
          <p className="text-xl text-[#E9E4D6]/70 mb-10">
            Commencez gratuitement avec 75 requêtes/mois. Aucune carte de crédit requise.
          </p>
          <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
            <Link 
              to="/signup"
              className="px-10 py-5 bg-[#D8B26A] text-[#1E1F22] font-bold text-xl rounded-xl hover:bg-[#E9E4D6] transition"
            >
              Créer mon compte gratuit
            </Link>
            <Link 
              to="/pricing"
              className="px-10 py-5 border-2 border-[#D8B26A] text-[#D8B26A] font-bold text-xl rounded-xl hover:bg-[#D8B26A]/10 transition"
            >
              Voir les tarifs
            </Link>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-12 px-6 bg-[#1E1F22] border-t border-[#D8B26A]/20">
        <div className="max-w-7xl mx-auto text-center">
          <p className="text-[#E9E4D6]/40 text-sm">
            © 2025 CHE·NU™. Tous droits réservés. | Gouvernance {'>'} Exécution
          </p>
        </div>
      </footer>
    </div>
  );
};

export default DemoPage;
