/* =====================================================
   CHE·NU — BOOTSTRAP MODULE
   Authority: FOUNDATION-LOCKED
   Mode: READ & OBEY
   
   Constitutional initialization protocol for all agents.
   These laws override optimization, performance,
   profit, and convenience.
   
   ❤️ With love, for humanity.
   ===================================================== */

export {
  // Authority types
  type BootstrapAuthority,
  type BootstrapMode,

  // Universal Bootstrap
  type UniversalBootstrap,
  type MandatoryBehavior,
  type ProhibitedBehavior,
  type ConflictResolutionProtocol,
  UNIVERSAL_BOOTSTRAP,

  // Contextual Bootstrap
  type ContextualBootstrap,
  createContextualBootstrap,

  // Internal Agent Bootstrap
  type AgentClass,
  type AgentPersistence,
  type AgentLearning,
  type InternalAgentBootstrap,
  type AgentPermission,
  type AgentProhibition,
  INTERNAL_AGENT_BOOTSTRAP,

  // Validation
  type BootstrapComplianceStatus,
  type BootstrapViolation,
  validateAgainstBootstrap,

  // Injection
  type BootstrapInjection,
  createBootstrapInjection,
  verifyBootstrapIntegrity,

  // Text generators
  generateUniversalBootstrapText,
  generateContextualBootstrapText,
  generateInternalAgentBootstrapText,
  generateFullBootstrapText,
} from './universalBootstrap.types';

// Re-export default
export { default } from './universalBootstrap.types';
